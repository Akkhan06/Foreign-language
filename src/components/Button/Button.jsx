import React from 'react';

const Button = ({title}) => {
    return (
        
            <button className="btn btn-warning  text-white">
             {title}
            </button>
        
    );
};

export default Button;